steps:
  - name: 'python:3.13-slim'
    id: Test
    entrypoint: bash
    args:
      - -c
      - |
        pip install -q -e ".[dev]" &&
        python -m pytest tests/ -q --tb=short -x
    env:
      - 'DATABASE_URL=sqlite+aiosqlite:///./test.db'
      - 'API_KEY=test-key'
      - 'ENVIRONMENT=test'

  - name: 'gcr.io/kaniko-project/executor:latest'
    id: Build
    waitFor: ['Test']
    args:
      - '--dockerfile=Dockerfile'
      - '--context=.'
      - '--destination=$_AR_HOSTNAME/$_AR_PROJECT_ID/$_AR_REPOSITORY/$REPO_NAME/$_SERVICE_NAME:$COMMIT_SHA'
      - '--cache=true'
      - '--cache-ttl=24h'
      - '--cache-repo=$_AR_HOSTNAME/$_AR_PROJECT_ID/$_AR_REPOSITORY/$_SERVICE_NAME-cache'
  - name: gcr.io/google.com/cloudsdktool/cloud-sdk:slim
    args:
      - run
      - services
      - update
      - $_SERVICE_NAME
      - '--platform=managed'
      - '--image=$_AR_HOSTNAME/$_AR_PROJECT_ID/$_AR_REPOSITORY/$REPO_NAME/$_SERVICE_NAME:$COMMIT_SHA'
      - '--labels=managed-by=gcp-cloud-build-deploy-cloud-run,commit-sha=$COMMIT_SHA,gcb-build-id=$BUILD_ID,gcb-trigger-id=$_TRIGGER_ID'
      - '--region=$_DEPLOY_REGION'
      - '--memory=2Gi'
      - '--cpu=2'
      - '--timeout=3600'
      - '--quiet'
    id: Deploy
    waitFor: ['Build']
    entrypoint: gcloud
options:
  substitutionOption: ALLOW_LOOSE
  logging: CLOUD_LOGGING_ONLY
substitutions:
  _AR_REPOSITORY: cloud-run-source-deploy
  _AR_PROJECT_ID: mvp-scraper-488408
  _PLATFORM: managed
  _SERVICE_NAME: mvp-backend
  _DEPLOY_REGION: europe-west1
  _TRIGGER_ID: 5c526d45-0c9c-499e-9917-4b7c7d2f80dc
  _AR_HOSTNAME: europe-west1-docker.pkg.dev
tags:
  - gcp-cloud-build-deploy-cloud-run
  - gcp-cloud-build-deploy-cloud-run-managed
  - mvp-backend